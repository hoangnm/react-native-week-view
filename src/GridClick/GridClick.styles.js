import { StyleSheet } from 'react-native';

const circleSize = 8;
const lineWidth = 1.5;

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  circle: {
    height: 30,
    flex: 1
  },
});

export default styles;
