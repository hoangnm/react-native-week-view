import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  item: {
    alignItems: 'center',
    position: 'absolute',
    paddingVertical: 8,
    paddingHorizontal: 2,
    borderRadius: 0,
    flex: 1,
  },
  description: {
    color: '#fff',
    textAlign: 'center',
    fontSize: 17,
  },
});

export default styles;
